You are a senior ML/CV implementation engineer.

You have already completed a paper-grounded dataset understanding phase for the SUSTech-SYSU corneal ulcer dataset using these two main papers:
1. diagnostics-14-00786.pdf
2. bioengineering-10-00639-v2.pdf

You also already produced:
- a paper-grounded summary
- a dataset manifest
- a schema map
- confirmed label-system reconstruction
- dataset audit findings
- leakage-risk findings
- split recommendations

Now your task is to generate the ACTUAL implementation artifacts for the EDA workflow.

==================================================
INPUTS AVAILABLE
==================================================
Assume the project contains:
- the full SUSTech-SYSU dataset
- the two paper PDFs
- the outputs from the previous analysis phase

You must re-use the previous phase outputs rather than reinventing dataset structure.

==================================================
PRIMARY GOAL
==================================================
Create a complete, runnable, modular Python EDA implementation for the SUSTech-SYSU corneal ulcer dataset, grounded in the earlier analysis and aligned with the two main papers.

The implementation must support both:
1. paper-faithful preprocessing-heavy analysis inspired by the Diagnostics paper
2. raw-image / feature-space analysis inspired by the Bioengineering paper

==================================================
REQUIRED OUTPUT FILES
==================================================
Generate the following project artifacts:

1. notebooks/
   - 01_paper_grounded_dataset_eda.ipynb

2. src/
   - config.py
   - utils_io.py
   - utils_viz.py
   - utils_image_stats.py
   - utils_duplicates.py
   - utils_embeddings.py
   - utils_preprocessing.py
   - utils_masks.py              (only if masks actually exist)
   - dataset_manifest.py
   - run_eda.py

3. outputs/
   - figures/
   - tables/
   - cache/
   - embeddings/
   - reports/

4. requirements-eda.txt

5. README_eda.md

==================================================
IMPLEMENTATION REQUIREMENTS
==================================================
The notebook and scripts must:

- use Python 3.10+
- use pathlib throughout
- use logging
- use tqdm for progress bars
- use robust exception handling for corrupt images/files
- avoid assuming file names unless discovered from manifest
- support GPU acceleration wherever available
- degrade gracefully to CPU

Preferred libraries:
- torch
- torchvision
- timm
- kornia
- opencv-python
- numpy
- pandas
- scikit-learn
- matplotlib
- pillow
- imagehash
- umap-learn
- pyarrow
- optionally cupy / cudf / cuml if installed

==================================================
NOTEBOOK STRUCTURE
==================================================
The notebook must be clean, thesis-grade, and divided into these sections:

1. Environment and reproducibility
2. Dataset discovery recap
3. Dataset audit
4. Label reconstruction
5. Raw-image statistics
6. Domain-aware fluorescein heuristics
7. Paper-inspired preprocessing comparison
8. Class-wise visual analysis
9. Duplicate / near-duplicate / leakage analysis
10. Embedding-space analysis
11. Small-data risk analysis
12. Split recommendations
13. Model-readiness summary
14. Final conclusions

==================================================
SCRIPT BEHAVIOR
==================================================
run_eda.py must:
- create output directories
- load config
- build or load manifest
- run all enabled EDA stages
- save tables and figures
- save intermediate caches
- export summary report artifacts

Support command-line flags such as:
- --data-root
- --output-root
- --batch-size
- --num-workers
- --device auto|cpu|cuda
- --skip-embeddings
- --skip-duplicates
- --skip-preprocessing
- --skip-mask-analysis
- --seed

==================================================
CONFIG REQUIREMENTS
==================================================
config.py should expose a central config with:
- dataset root
- output root
- random seed
- image extensions
- batch size
- num workers
- figure DPI
- sample counts for montages
- embedding backbones to use
- preprocessing variants to compare
- whether masks are available
- whether RAPIDS is available
- thresholds for duplicate detection
- thresholds for blur/brightness heuristics

==================================================
EDA REQUIREMENTS TO IMPLEMENT
==================================================

A. Dataset manifest
- enumerate all files
- map image files to labels/masks/metadata where possible
- save manifest as CSV and Parquet

B. Dataset audit
- count files by type
- detect missing/corrupt files
- detect inconsistent file naming
- detect unreadable images
- summarize folder structure

C. Label reconstruction
- rebuild all confirmed label systems from actual dataset files
- compute class distributions
- save tables

D. Raw-image statistics
- resolution
- aspect ratio
- file size
- channel mean/std
- brightness
- contrast
- blur proxy
- entropy
- color summaries
- green dominance metrics

E. Domain-aware heuristics
- green stain coverage proxy
- center/periphery intensity proxy
- specular highlight proxy
- rough foreground/cornea area proxy
- do not overclaim clinical interpretation

F. Paper-inspired preprocessing comparison
Implement these representations where possible:
- raw RGB
- blue-channel-removed
- grayscale
- Gaussian blur
- Otsu threshold
- masked/proxy highlighted ulcer region
- optional CLAHE as exploratory but clearly separate

For each:
- visualize representative outputs
- compute summary shifts
- compare class-separability heuristics

G. Visual EDA
- stratified image grids
- class-wise montages
- extreme examples
- outlier examples
- optional mask overlays if masks exist

H. Duplicate and leakage checks
- exact duplicate hashes
- perceptual hashes
- embedding similarity
- cross-label duplicate suspicion
- cross-split duplicate suspicion if official splits exist

I. Embedding-space EDA
Use GPU batched inference where possible.
At minimum generate embeddings from:
- ResNet-18
- one stronger CNN
- one ViT

Then:
- UMAP and/or t-SNE
- cluster plots by label system
- outlier detection
- nearest-neighbor retrieval examples
- compare raw vs selected preprocessed variants

J. Small-data risk section
Generate programmatic summaries for:
- class imbalance
- tiny classes
- instability risk
- leakage risk
- split fragility

K. Split recommendation export
Create a final markdown report that recommends safe future split strategies for each task.

==================================================
SAVE ARTIFACTS
==================================================
Save:
- all plots to outputs/figures
- all tables to outputs/tables
- embeddings to outputs/embeddings
- caches to outputs/cache
- final written summaries to outputs/reports

Also export:
- outputs/reports/eda_summary.md
- outputs/reports/leakage_report.md
- outputs/reports/split_recommendations.md

==================================================
README REQUIREMENTS
==================================================
Create README_eda.md containing:
- project purpose
- environment setup
- dependency installation
- how to run notebook
- how to run run_eda.py
- expected folder structure
- common failure points
- GPU notes
- what each output folder contains

==================================================
ANTI-HALLUCINATION RULES
==================================================
- Do not invent dataset structure.
- Do not invent label mappings.
- Only analyze mask behavior if masks truly exist.
- Do not report model metrics unless a model was actually trained.
- Distinguish clearly between:
  - paper claims
  - dataset facts
  - EDA-computed findings

==================================================
DELIVERY STYLE
==================================================
Return:
1. a short implementation plan
2. the full code for all files
3. the notebook cell structure
4. commands to run the workflow
5. notes on where manual adaptation may still be needed if the dataset schema differs

Start by using the earlier manifest and summary, then generate the full implementation.