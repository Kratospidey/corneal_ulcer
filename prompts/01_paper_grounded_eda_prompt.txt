You are a senior computer-vision research engineer and medical-image EDA specialist.

Your task is to perform a paper-grounded, dataset-faithful, thorough exploratory data analysis (EDA) of the SUSTech-SYSU corneal ulcer dataset using Python, while first deeply understanding the two attached main papers:

1) diagnostics-14-00786.pdf
2) bioengineering-10-00639-v2.pdf

Your job is NOT to give a shallow generic EDA.
You must first extract the important methodological nuances, label definitions, preprocessing assumptions, evaluation framing, and limitations from BOTH papers, and then use that understanding to drive a rigorous EDA of the actual dataset on disk.

==================================================
PRIMARY GOAL
==================================================
Produce a complete, reproducible, research-grade Python EDA workflow for the SUSTech-SYSU corneal ulcer dataset that:
- is explicitly informed by both attached papers,
- respects the actual dataset structure instead of assuming it,
- uses GPU-accelerated libraries wherever realistically available,
- highlights dataset strengths, weaknesses, label structure, preprocessing implications, leakage risks, and model-readiness,
- prepares the ground for later experiments with both:
  (a) raw-image feature-learning / transfer-learning style pipelines,
  (b) preprocessing-heavy / masked-image style pipelines.

==================================================
NON-NEGOTIABLE RULES
==================================================
1. FIRST read and understand both PDFs before doing any coding.
2. DO NOT hallucinate dataset structure, filenames, folders, labels, masks, or CSV schema.
   Inspect the actual dataset contents first and infer the true layout.
3. DO NOT blindly trust paper-reported counts over the actual files.
   Treat the papers as guidance and the dataset files as source of truth.
4. DO NOT apply augmentation before reasoning about data splitting and leakage.
   If you discuss augmentation, distinguish clearly between:
   - EDA-time visualization augmentation
   - train-time augmentation
5. Use Python only.
6. Prefer GPU libraries where available, but degrade gracefully to CPU if not installed.
7. Save all intermediate outputs, tables, and figures in a clean project structure.
8. Write robust, modular, runnable code with comments, logging, and clear sectioning.
9. If anything is ambiguous in the dataset, explicitly state uncertainty and show the evidence.
10. Avoid fake conclusions. Everything must be grounded in either:
   - the papers,
   - the actual dataset files,
   - or computed statistics.

==================================================
PAPER-UNDERSTANDING PHASE
==================================================
Before coding, extract and summarize the following from BOTH papers:

A. From diagnostics-14-00786.pdf:
- the task framing,
- the three scenario definitions,
- the class definitions for each scenario,
- the preprocessing pipeline,
- the augmentation pipeline,
- the evaluation metrics,
- the model family used,
- any important assumptions or weaknesses.

B. From bioengineering-10-00639-v2.pdf:
- the raw-image classification philosophy,
- why small dataset size matters,
- the ResNet-18 / feature-extraction / feature-selection logic,
- the SVM usage,
- what parts of the pipeline avoid heavy fine-tuning,
- any split assumptions,
- any methodological caveats,
- what this implies for feature-space EDA on a small medical-image dataset.

Then provide a short synthesis:
- What both papers agree on
- What differs between them
- What EDA must check because of these differences

Important:
The Diagnostics paper should make you sensitive to:
- multi-scenario labels,
- preprocessing effects,
- masking logic,
- augmentation effects,
- severity framing.

The Bioengineering paper should make you sensitive to:
- small-sample behavior,
- raw-vs-processed image representations,
- transfer-learning feature quality,
- feature redundancy,
- separability in latent space,
- and the danger of over-claiming from a small dataset.

==================================================
DATASET DISCOVERY PHASE
==================================================
Inspect the dataset root and build a clear manifest of:
- folder tree,
- image folders,
- label files,
- mask files or annotation files if present,
- train/val/test split files if present,
- metadata files,
- README or documentation files if present,
- file extensions,
- counts per file type.

Output:
1. A dataset inventory table
2. A schema map explaining how images, labels, and masks connect
3. A list of any missing or inconsistent items
4. A note on whether the actual dataset supports:
   - 3-class ulcer pattern classification
   - 5-class TG/type-grade analysis
   - 5-class severity analysis
   - segmentation/mask-based study
   - binary ulcer-vs-non-ulcer study

Do not assume these are present just because a paper used them.
Verify.

==================================================
GPU / ENVIRONMENT REQUIREMENTS
==================================================
Use GPU-accelerated tools wherever practical and available.

Preferred stack:
- Python 3.10+
- PyTorch + torchvision
- timm
- Kornia for GPU image transforms and filtering
- OpenCV (and cv2.cuda if available)
- CuPy where helpful
- RAPIDS cuDF / cuML if available for fast tabular aggregation / dimensionality reduction
- numpy / pandas as fallback
- scikit-learn
- matplotlib
- plotly optional
- seaborn optional only if already present, otherwise skip
- pillow
- imagehash or perceptual hashing library
- tqdm
- pyarrow / parquet support if useful

Rules:
- Detect CUDA availability at runtime.
- Print a hardware/environment summary at the start.
- Use batched GPU inference for embeddings.
- If a preferred GPU package is missing, fall back cleanly rather than failing.

==================================================
REQUIRED EDA SECTIONS
==================================================

SECTION 1 — Environment and reproducibility
- Print Python version, OS, CUDA availability, GPU name, VRAM if accessible
- Set all random seeds
- Define project folders:
  /outputs
  /outputs/figures
  /outputs/tables
  /outputs/cache
  /outputs/embeddings
  /outputs/reports

SECTION 2 — Dataset audit
- Total number of images
- Number of masks / annotations if present
- Missing files
- Corrupt/unreadable files
- Duplicate filenames
- Inconsistent naming patterns
- Distribution by folder and label source
- A manifest CSV/Parquet

SECTION 3 — Label-system reconstruction
Using the actual files, reconstruct all available label systems.
Try to map the dataset to the paper-inspired tasks, for example:
- ulcer pattern / type
- TG / type-grade
- severity grade
- ulcer-vs-non-ulcer if feasible

For every label system that exists:
- show class names,
- class counts,
- imbalance ratios,
- minority/majority ratios,
- entropy / class-balance metrics,
- whether any classes are dangerously tiny,
- whether any labels appear derived from masks or metadata.

Cross-check paper expectations against actual file counts.
If they mismatch, say so clearly.

SECTION 4 — Core image statistics
For the raw images:
- resolution distribution
- aspect ratio distribution
- file size distribution
- bit depth / mode if accessible
- RGB channel means/stds
- per-channel histograms
- brightness distribution
- contrast distribution
- saturation / hue summaries if useful
- blur/sharpness proxies (e.g. variance of Laplacian)
- entropy
- highlight/glare proxies
- green-fluorescein dominance statistics
- center-of-mass / intensity concentration heuristics

Create:
- sample image grids
- class-wise montages
- random stratified examples
- extreme examples (darkest, brightest, blurriest, highest green intensity, etc.)

SECTION 5 — Cornea-centric morphology and region heuristics
Because these are fluorescein-stained slit-lamp images, add domain-aware heuristics:
- green-region coverage estimates
- central-vs-peripheral stain concentration
- rough corneal bounding/circularity heuristics if possible
- background vs cornea area ratio
- reflection/specular highlight prevalence
- left/right eye orientation hints if detectable

Do not overclaim clinical meaning.
Frame these as image-derived heuristics, not clinical diagnoses.

SECTION 6 — Paper-inspired preprocessing comparison
Implement and compare at least these representations:

A. Raw RGB images
B. Blue-channel-removed images
C. grayscale-converted images
D. Gaussian-blurred images
E. Otsu-thresholded images
F. masked / highlighted ulcer-region proxy images if reproducible from the dataset
G. optionally CLAHE / adaptive histogram variants if useful, but keep them clearly separate from paper-faithful steps

For each representation:
- save representative sample grids
- compute summary statistics
- compare class separability heuristics
- compare histogram shifts
- compare green-region behavior
- note whether preprocessing destroys useful context or improves signal isolation

Important:
Make this section explicitly paper-aware:
- Diagnostics-style preprocessing path
- Bioengineering-style raw-image path

SECTION 7 — Class-wise visual EDA
For each available label scheme:
- class-balanced montage
- nearest-neighbor visual grids within class
- borderline-looking samples across classes
- hard negatives / visually overlapping classes
- outlier examples
- mislabeled-suspect examples if any

If masks exist:
- overlay masks on raw images
- compare mask area / perimeter / compactness across classes
- analyze mask coverage vs severity labels
- analyze whether masks and severity are consistent

SECTION 8 — Similarity, duplicates, and leakage risk
This is critical.

Perform:
- exact duplicate detection via hashes
- near-duplicate detection via perceptual hashes
- embedding-based similarity search
- duplicate groups across folders / splits / labels
- cluster-based suspicion of repeated or near-repeated acquisitions
- leakage risk report

If official splits exist:
- explicitly test whether duplicates or near-duplicates cross split boundaries
If no official splits exist:
- recommend leakage-safe split strategies

Also discuss:
- patient-level leakage cannot be ruled out if patient IDs are absent
- filename-based leakage risks
- augmentation-before-split risks

SECTION 9 — Embedding-space EDA
Use pretrained feature extractors and generate embeddings for the raw images and optionally selected preprocessed variants.

At minimum:
- ResNet-18 embeddings
- one stronger CNN embedding if available (e.g. ConvNeXt / EfficientNet)
- one ViT embedding if available (e.g. ViT / DeiT from timm)

Use GPU inference in batches.

Then:
- reduce dimensions using UMAP or t-SNE
  - prefer GPU UMAP if RAPIDS is available
  - otherwise use CPU fallback
- plot embeddings colored by each label system
- inspect cluster separation
- identify mixed clusters
- identify outliers
- identify classes that are visually entangled
- compare raw-vs-preprocessed embedding separation

This section must explicitly connect back to:
- the ResNet feature-space perspective from the Bioengineering paper
- the ViT classification framing from the Diagnostics paper

SECTION 10 — Small-data risk analysis
Because this dataset is small, include a dedicated section on:
- overfitting risk
- imbalance risk
- overconfident metrics risk
- instability under random splits
- augmentation leakage risk
- preprocessing leakage risk
- whether 5-fold CV vs fixed split may be more appropriate
- whether binary, 3-class, or 5-class tasks are realistically stable
- which classes are too underpowered for strong claims

SECTION 11 — Split strategy recommendations
Propose careful experimental split strategies for later modeling:
- stratified split by label
- repeated stratified splits
- cross-validation recommendations
- duplicate-aware split
- near-duplicate-aware split
- if metadata supports it, patient-aware split
- augmentation only after split
- preserve untouched test set

Give recommended split plans separately for:
- 3-class ulcer pattern task
- 5-class type-grade task
- 5-class severity task
- binary task if supported

SECTION 12 — Metric recommendations
For later modeling, recommend metrics appropriate to imbalance and medical imaging:
- balanced accuracy
- macro F1
- weighted F1
- one-vs-rest ROC-AUC where valid
- PR-AUC for minority classes
- confusion matrix
- calibration checks
- per-class recall emphasis

Explain which are most trustworthy for each task.

SECTION 13 — Baseline experiment readiness notes
Do NOT fully train large models unless explicitly asked.
But provide a model-readiness summary informed by the EDA:
- raw-image transfer-learning baseline readiness
- preprocessing-heavy baseline readiness
- ResNet-18 baseline suitability
- ViT baseline suitability
- whether masks suggest segmentation-assisted classification may help
- whether class imbalance requires weighted loss / focal loss / oversampling
- whether stain-area features may complement learned embeddings

SECTION 14 — Final research conclusions
End with a concise but serious research summary:
1. What the dataset appears suitable for
2. What it is not robust enough for
3. Which paper-style pipeline seems more plausible for this dataset and why
4. Which failure modes are most likely
5. What should be fixed before any serious benchmark is reported

==================================================
OUTPUT FORMAT
==================================================
You must produce:

1. A short paper-grounded summary before coding
2. Then the full Python EDA implementation
3. Then the executed findings / interpreted results
4. Then a final research recommendation section

If execution is possible in the environment:
- run the code
- save plots and tables
- report actual results

If execution is not possible:
- still provide complete runnable code
- clearly separate “implemented code” from “unexecuted expectations”

==================================================
CODE QUALITY REQUIREMENTS
==================================================
- Use functions and clear modular structure
- Include type hints where reasonable
- Use pathlib
- Use logging
- Use tqdm for long loops
- Cache expensive computations
- Avoid loading all images into RAM if unnecessary
- Use PyTorch DataLoader for batched processing
- Save intermediate embeddings
- Save final tables as CSV and preferably Parquet
- Save figures at publication-friendly resolution
- Handle corrupt files gracefully
- Add comments explaining why each analysis matters

==================================================
SPECIAL INSTRUCTIONS ON PAPER-FAITHFULNESS
==================================================
Your EDA must explicitly reflect BOTH attached papers:

From diagnostics-14-00786.pdf:
- three-scenario framing
- preprocessing pipeline:
  remove blue channel -> grayscale -> Gaussian blur -> Otsu threshold -> masking
- augmentation ideas:
  rotation, scaling, padding, flipping
- ViT-oriented downstream readiness

From bioengineering-10-00639-v2.pdf:
- raw-image pathway must remain alive
- small-dataset caution
- pretrained ResNet-18 feature extraction relevance
- feature-space analysis is especially important
- do not assume aggressive preprocessing is always better
- analyze whether raw embeddings already separate classes reasonably well

So:
- compare raw and processed representations side by side
- compare CNN and ViT embedding spaces
- treat preprocessing as a hypothesis to be tested, not a guaranteed improvement

==================================================
ANTI-HALLUCINATION / ANTI-SLOPPINESS RULES
==================================================
- Never invent class mappings.
- Never invent mask availability.
- Never invent split files.
- Never say “the dataset contains X” unless verified from files.
- Never say preprocessing improved separability unless supported by computed evidence.
- Never present paper-reported metrics as if they were your dataset results.
- Always distinguish:
  - “paper reports”
  - “dataset actually contains”
  - “our EDA computed”

==================================================
FINAL DELIVERABLE STYLE
==================================================
Write like a serious ML research engineer preparing a reproducible EDA notebook for a thesis or publishable project.
Be precise, skeptical, and evidence-driven.
Do not be vague.
Do not skip difficult checks.
Do not stop at bar charts.

Start now by:
1. reading and summarizing both PDFs,
2. inspecting the dataset structure,
3. building the dataset manifest,
4. and only then proceeding into full EDA.