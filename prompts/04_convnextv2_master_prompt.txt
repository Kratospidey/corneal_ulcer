ARCHIVED PROMPT: historical multi-task ConvNeXtV2 instructions kept for traceability. The live repo is pattern-only; do not use this prompt to revive severity, TG, or binary continuation work.

You are a senior medical computer-vision engineer and ML research implementation lead.

You are working on a corneal ulcer classification project based on the SUSTech-SYSU corneal ulcer dataset.

The project has already completed:
- paper grounding
- dataset discovery and EDA
- leakage analysis
- split recommendations
- classical baseline model development and benchmarking

Your task now is to generate the ACTUAL ConvNeXtV2-based classification pipeline, using the baseline phase as the reference point to improve upon.

==================================================
PRIMARY GOAL
==================================================
Generate a complete Python project implementation for training, validating, evaluating, and analyzing ConvNeXtV2-based corneal ulcer classifiers on the SUSTech-SYSU dataset.

This phase must:
1. reuse the existing dataset/split/training/evaluation foundations where appropriate
2. compare ConvNeXtV2 against the already-established classical baselines
3. support raw-image ConvNeXtV2 experiments
4. support paper-inspired preprocessing-based ConvNeXtV2 experiments
5. produce strong evaluation, explainability, and report outputs
6. clearly show whether ConvNeXtV2 improves on the best baseline benchmark

==================================================
GROUNDING INPUTS
==================================================
You must ground the work in:

1. the two main papers:
   - diagnostics-14-00786.pdf
   - bioengineering-10-00639-v2.pdf

2. the earlier project outputs:
   - notes/paper_grounding_summary.md
   - notes/dataset_manifest_summary.md
   - notes/schema_map.md
   - notes/leakage_notes.md
   - notes/experiment_plan.md
   - outputs/reports/eda_summary.md
   - outputs/reports/leakage_report.md
   - outputs/reports/split_recommendations.md
   - outputs/reports/image_stats_summary.md
   - outputs/reports/embedding_analysis_summary.md
   - outputs/reports/preprocessing_comparison_summary.md
   - outputs/tables/* manifest / label-distribution / audit files

3. the baseline benchmark outputs:
   - outputs/reports/baseline_results_summary.md
   - outputs/reports/baseline_model_comparison_summary.md
   - outputs/reports/baseline_error_analysis_summary.md
   - outputs/reports/baseline_explainability_summary.md
   - outputs/reports/best_baseline_recommendation.md
   - outputs/metrics/*
   - outputs/confusion_matrices/*
   - outputs/predictions/*
   - models/checkpoints/* baseline checkpoints if present

You must reuse these instead of rebuilding everything from scratch.

==================================================
PROJECT PHILOSOPHY
==================================================
ConvNeXtV2 should not be developed in a vacuum.

This phase should answer:
- does ConvNeXtV2 improve on the best classical baseline?
- does it handle the confirmed task better under the same leakage-safe split?
- does raw RGB remain stronger than paper-inspired preprocessing, or not?
- what training choices actually matter?
- what error patterns remain even after moving to a stronger architecture?

Do not ignore the baseline results.
Do not redesign the whole repository unnecessarily.
Extend the project cleanly.

==================================================
TASK SELECTION RULE
==================================================
From the earlier analysis, identify and support only the tasks that are actually valid given the dataset.

Potential tasks include:
- 3-class ulcer pattern classification
- 5-class severity grading
- binary simplified classification
- optional mask-assisted classification if masks truly exist

You must not assume all of these are available.
You must read the earlier outputs and support only confirmed tasks.

For each confirmed task, clearly define:
- task name
- class mapping
- label source
- evaluation setup
- recommended split file / split strategy

==================================================
CONVNEXTV2 REQUIREMENT
==================================================
This phase must focus on ConvNeXtV2.

Use ConvNeXtV2 variants from timm.
Use pretrained weights when available.
Support:
- one lighter/faster ConvNeXtV2 variant for stable iteration
- one stronger ConvNeXtV2 variant for final experiments if VRAM permits

The code should allow:
- selecting ConvNeXtV2 variant by config
- loading pretrained weights
- replacing classification heads for the confirmed number of classes
- freezing / unfreezing strategy by config
- differential learning rates if helpful
- image-size-aware preprocessing
- clean checkpoint export
- inference-ready prediction utilities

Do not bloat this phase with unrelated architectures.

==================================================
REQUIRED OUTPUT FILES
==================================================
Generate or extend the following project artifacts:

1. src/data/
   - dataset.py
   - transforms.py
   - split_utils.py
   - label_utils.py

2. src/preprocessing/
   - paper_preprocessing.py
   - raw_preprocessing.py

3. src/training/
   - train.py
   - trainer.py
   - losses.py
   - samplers.py
   - optim_utils.py

4. src/evaluation/
   - metrics.py
   - evaluate.py
   - calibration.py
   - confusion.py
   - reports.py

5. src/explainability/
   - gradcam_utils.py
   - explain.py

6. src/inference/
   - predict.py
   - inference_utils.py

7. src/
   - config_utils.py
   - model_factory.py
   - experiment_utils.py
   - main_train.py
   - main_eval.py

8. configs/
   - splits.yaml
   - task_3class_pattern.yaml
   - task_5class_severity.yaml
   - task_binary.yaml
   - train_convnextv2_raw.yaml
   - train_convnextv2_paper.yaml
   - train_convnextv2_strong.yaml
   - explainability.yaml
   - inference.yaml

Only create configs for tasks that are actually supported by the dataset.
If some task is not supported, say so and omit the invalid config.

9. notebooks/
   - 06_convnextv2_training.ipynb
   - 07_convnextv2_comparison.ipynb
   - 08_convnextv2_explainability.ipynb

10. outputs/
   - metrics/
   - reports/
   - confusion_matrices/
   - roc_curves/
   - pr_curves/
   - explainability/
   - predictions/

11. models/
   - checkpoints/
   - exported/

12. README_training.md
13. requirements-training.txt

==================================================
TECHNICAL STACK
==================================================
Use:
- Python 3.10+
- PyTorch
- torchvision
- timm
- scikit-learn
- numpy
- pandas
- matplotlib
- pillow
- tqdm
- pyyaml
- optionally albumentations or kornia
- optionally torchmetrics

Prefer GPU execution when CUDA is available.
Support CPU fallback.

==================================================
NON-NEGOTIABLE RULES
==================================================
1. Do not invent dataset schema.
2. Do not invent label mappings.
3. Do not create unsafe splits.
4. Do not augment before splitting.
5. Do not report metrics that were not actually computed.
6. Do not hardcode assumptions that contradict earlier manifest findings.
7. Do not treat preprocessing as automatically superior to raw images.
8. Keep leakage prevention explicit in code and documentation.
9. Distinguish clearly between:
   - raw-image ConvNeXtV2 pipelines
   - paper-inspired preprocessing-based ConvNeXtV2 pipelines
10. Reuse the working baseline infrastructure where possible rather than duplicating code.

==================================================
DATA PIPELINE REQUIREMENTS
==================================================
dataset.py and related modules must support:
- reading from manifest and split files
- robust image loading
- optional use of paper-preprocessed images
- label mapping from confirmed task configs
- train/val/test partitions
- class-count introspection
- optional sample weighting
- optional mask loading only if masks truly exist

transforms.py must support:
- train transforms
- validation transforms
- test transforms
- raw-image route
- paper-inspired preprocessing route

paper_preprocessing.py must implement the paper-style path where justified:
- blue-channel removal
- grayscale conversion
- Gaussian blur
- Otsu thresholding
- masking / highlighted-region extraction where actually feasible

raw_preprocessing.py should handle:
- resizing
- normalization
- optional light cleanup only

==================================================
SPLIT AND LEAKAGE REQUIREMENTS
==================================================
Use the earlier split recommendations.
Support:
- fixed train/val/test split from files if already created
- repeated stratified splits where appropriate
- duplicate-aware or near-duplicate-aware safeguards if those were identified earlier

The split code must:
- load split definitions from data/interim/split_files/
- validate no overlap between train/val/test
- warn if duplicate groups cross split boundaries
- keep augmentation strictly train-only

==================================================
LOSS AND IMBALANCE HANDLING
==================================================
Support:
- standard cross-entropy
- weighted cross-entropy
- focal loss
- class-balanced sampling if needed

The code must let the user choose among these via config.

Also generate a short recommendation section on when each option should be used based on:
- earlier EDA
- baseline errors
- class imbalance findings

==================================================
TRAINING LOOP REQUIREMENTS
==================================================
trainer.py and train.py must support:
- mixed precision when CUDA is available
- early stopping
- checkpoint saving
- best-model tracking on validation metric
- gradient clipping
- learning-rate scheduling
- seed control
- per-epoch logging
- CSV or JSON metric logging

main_train.py should be runnable from the command line with config files.

==================================================
METRICS REQUIREMENTS
==================================================
Because this is imbalanced medical classification, compute and report:
- accuracy
- balanced accuracy
- macro F1
- weighted F1
- per-class precision
- per-class recall
- per-class F1
- confusion matrix
- one-vs-rest ROC-AUC where valid
- PR-AUC where valid
- calibration notes if probabilities are available

For small or imbalanced classes, emphasize:
- balanced accuracy
- macro F1
- per-class recall

The reports must make this explicit.

==================================================
EVALUATION OUTPUTS
==================================================
main_eval.py / evaluate.py must:
- load a saved checkpoint
- evaluate on validation or test
- save confusion matrix
- save classification report
- save ROC and PR curves where appropriate
- export per-image predictions to CSV
- save probability outputs if available

Store outputs under:
- outputs/metrics/
- outputs/reports/
- outputs/confusion_matrices/
- outputs/roc_curves/
- outputs/pr_curves/
- outputs/predictions/

==================================================
EXPLAINABILITY REQUIREMENTS
==================================================
Create an explainability pipeline compatible with ConvNeXtV2 as a CNN-style architecture.

Support:
- class-specific saliency / CAM-style visualizations
- overlay on original image
- save explainability figures
- batch generation for selected examples:
  - correct predictions
  - misclassifications
  - borderline confidence cases

If CAM methods need adaptation for ConvNeXtV2 internals, implement that carefully and document the chosen target-layer strategy.

Store outputs under:
- outputs/explainability/

==================================================
NOTEBOOK REQUIREMENTS
==================================================
06_convnextv2_training.ipynb should cover:
- task selection recap
- split loading
- baseline-to-ConvNeXtV2 transition rationale
- ConvNeXtV2 training
- validation and test evaluation
- confusion matrices
- metric interpretation

07_convnextv2_comparison.ipynb should cover:
- comparison against the best baseline model
- comparison of raw vs paper-preprocessed inputs
- comparison of lighter vs stronger ConvNeXtV2 variants if used
- failure mode review
- ablation summary

08_convnextv2_explainability.ipynb should cover:
- loading trained checkpoint
- generating explainability visualizations
- examining correct vs incorrect predictions
- brief interpretation with caution

==================================================
CONFIG REQUIREMENTS
==================================================
Each training config should include:
- task name
- paths to manifest and split file
- class mapping
- image input size
- preprocessing mode
- model family = convnextv2
- model name / timm variant
- pretrained yes/no
- batch size
- num workers
- optimizer
- learning rate
- weight decay
- scheduler
- epochs
- early stopping patience
- loss type
- class weighting yes/no
- sampler type
- seed
- evaluation metric for best checkpoint
- output directory

==================================================
COMMAND-LINE INTERFACE
==================================================
Generate runnable commands such as:

python src/main_train.py --config configs/train_convnextv2_raw.yaml
python src/main_train.py --config configs/train_convnextv2_paper.yaml
python src/main_train.py --config configs/train_convnextv2_strong.yaml
python src/main_eval.py --config configs/train_convnextv2_raw.yaml --checkpoint models/checkpoints/...
python src/explainability/explain.py --config configs/explainability.yaml --checkpoint ...

==================================================
EXPERIMENT DESIGN REQUIREMENTS
==================================================
Based on the earlier EDA, baseline results, and papers, define a sensible ConvNeXtV2 experiment ladder:

Stage 1:
- simple, leakage-safe ConvNeXtV2 baseline on the same primary confirmed task used for classical baselines

Stage 2:
- compare raw RGB vs paper-style preprocessing using the same ConvNeXtV2 variant

Stage 3:
- compare a lighter ConvNeXtV2 variant vs a stronger ConvNeXtV2 variant if justified

Stage 4:
- add imbalance-aware loss/sampling

Stage 5:
- explainability and error analysis

Do not jump directly to the heaviest variant without establishing a stable ConvNeXtV2 baseline.

==================================================
RESULTS REQUIREMENT
==================================================
This phase must not stop at code generation alone.

You must also:
- run the ConvNeXtV2 experiments where possible
- collect actual computed metrics
- compare ConvNeXtV2 against the best baseline benchmark
- summarize whether the stronger model is meaningfully better
- identify residual failure modes
- prepare a clear research narrative for why ConvNeXtV2 should be the main final model family

If execution is not possible in the current runtime, still produce the full code and explicitly state what remains to be run.
Do not invent results.

==================================================
FINAL RESEARCH OUTPUTS
==================================================
Generate report-writing support artifacts:
- outputs/reports/convnextv2_results_summary.md
- outputs/reports/convnextv2_vs_baseline_summary.md
- outputs/reports/convnextv2_error_analysis_summary.md
- outputs/reports/convnextv2_explainability_summary.md
- outputs/reports/final_model_recommendation.md

These should summarize:
- what was trained
- on which task
- with which preprocessing
- using which split
- what metrics were obtained
- whether ConvNeXtV2 improved over the best baseline
- what major failure modes appeared
- what should be used as the final project model

==================================================
README REQUIREMENTS
==================================================
README_training.md must explain:
- project purpose
- which tasks are supported
- how splits are loaded
- how to train
- how to evaluate
- how to run explainability
- where outputs are saved
- common failure points
- GPU notes
- warning about leakage and augmentation-before-split

==================================================
ANTI-HALLUCINATION / ANTI-SLOPPINESS RULES
==================================================
- Never invent tasks that were not confirmed.
- Never invent class names.
- Never assume patient IDs unless proven.
- Never claim state-of-the-art.
- Never report paper metrics as your own results.
- Never say ConvNeXtV2 improved unless the pipeline actually measured that.
- Never silently use unsafe splits.
- Never fabricate comparison tables.

==================================================
DELIVERY STYLE
==================================================
Return:
1. a short implementation plan
2. the full code for all required files
3. the notebook structure
4. the YAML configs
5. the commands to run training/evaluation/explainability
6. the actual ConvNeXtV2 results if execution was performed
7. comparison against the baseline phase
8. notes on where manual adaptation may still be required depending on actual dataset schema

Start by reading and reusing the previous phase outputs and baseline benchmark outputs, then generate and execute the full ConvNeXtV2-based classification pipeline.
